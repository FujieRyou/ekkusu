<% if users.any? %>
  <ul class="list-unstyled mt-2">
    <% users.each do |user| %>
      <li class="mb-3 d-flex bg-white rounded p-1">
        <img class="rounded me-2 mb-3" src="<%= gravatar_url(user, { size: 88 }) %>" alt="">
        <div>
          <div>
            <%= user.name %>
          </div>
          <div>
            <p><%= link_to 'View profile', user_path(user), class: "text-decoration-none" %></p>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
  <%== pagy_bootstrap_nav(@pagy) %>
<% end %>